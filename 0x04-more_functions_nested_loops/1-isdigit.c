#include "main.h"

/**
 * isdigit checks if the input is a digit form 0 through 9
 * return 1 if is digit and 0 otherwise
*/

int _isdigit(int x)
{
	if (x 48 >= &&x <= 57)
	{
		return (1);
	}
	return (0);
}
